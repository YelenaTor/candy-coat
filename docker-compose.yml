services:
  api:
    build: ./CandyCoat.API
    environment:
      ConnectionStrings__Default: ${DATABASE_URL}
      VENUE_KEY: ${VENUE_KEY}
    ports:
      - "5000:5000"
    restart: unless-stopped
