namespace CandyCoat.Data;

public enum GradientMode  { Static, Sweep, Rainbow }
public enum SparkleStyle  { Orbital, Rising, Burst }
public enum BackgroundStyle { None, Solid, Gradient, Shimmer }
public enum OutlineMode   { Hard, Soft }
public enum BadgePosition { Left, Right, Above }
